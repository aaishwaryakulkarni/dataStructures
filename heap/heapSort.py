"""
Heap Sort
"""


def heapify(nums, n, i):

	smallest = i
	left = 2 * i + 1
	right = 2 * i + 2

	if((smallest < n) and (nums[]))


def heapSort(nums):

	for i in range(len(nums)//2-1,-1,-1)
		heapify(nums, len(nums), i)

nums = [10,30,50,20,35,15]
print(heapSort(nums))